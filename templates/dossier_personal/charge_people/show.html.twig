{% extends 'base.html.twig' %}

{% block title %}ChargePersonal{% endblock %}

{% block body %}
    <h1>ChargePersonal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ charge_personal.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ charge_personal.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ charge_personal.lastName }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ charge_personal.birthday ? charge_personal.birthday|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ charge_personal.genre }}</td>
            </tr>
            <tr>
                <th>RefPiece</th>
                <td>{{ charge_personal.refPiece }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ charge_personal.contact }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ charge_personal.uuid }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ charge_personal.createdAt ? charge_personal.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ charge_personal.updatedAt ? charge_personal.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('charge_people_index') }}">back to list</a>

    <a href="{{ path('charge_people_edit', {'id': charge_personal.id}) }}">edit</a>

    {{ include('dossier_personal/charge_people/_delete_form.html.twig') }}
{% endblock %}
