{% block _account_accountBanks_entry_row %}
    {{ form_widget(form) }}
{% endblock %}

{% block _account_accountBanks_widget %}
    <div class="col-md-12">
        <div class="form-group pull-right">
            <button type="button"
                    data-remove-target="block_{{ id }}"
                    data-tr-id="{{ id }}"
                    data-collection-holder-class="#account_accountBanks"
                    data-target="#detail_personal_account_banque_table"
                    data-index="{{ form | length > 0 ? form | last.vars.name + 1 : 0 }}"
                    class="btn btn-outline-secondary add_item_link mb-2 float-end">
                Nouvelle ligne
            </button>
        </div>
    </div>
    {{ form_widget(form) }}
{% endblock %}

{% block _account_accountBanks_entry_widget %}
    <tr id="block_{{ id }}" data-id="{{ id }}" class="data-row">
        <td>
            {{ form_widget(form.bankId,{'attr': {
                "class": 'form-control form-control-sm',
            }}) }}
        </td>
        <td>
            {{ form_widget(form.code,{'attr': {
                "class": 'form-control form-control-sm',
            }}) }}
        </td>
        <td>
            {{ form_widget(form.numCompte,{'attr': {
                "class": 'form-control form-control-sm',
            }}) }}
        </td>
        <td>
            {{ form_widget(form.rib,{'attr': {
                "class": 'form-control form-control-sm',
            }}) }}
        </td>
        <td class="text-center">
            <button type="button"
                class="btn btn-danger btn-sm btn-rounded del_item_link"
                data-action="delete"
                data-target="#block_{{ id }}">
                <i class="bi bi-trash"></i>
            </button>
        </td>
    </tr>
{% endblock %}
