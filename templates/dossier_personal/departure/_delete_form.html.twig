<form method="post" action="{{ path('app_dossier_personal_departure_delete', {'id': departure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departure.id) }}">
    <button class="btn">Delete</button>
</form>
