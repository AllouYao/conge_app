{% extends 'base.html.twig' %}

{% block title %}CategorySalarie{% endblock %}

{% block body %}
    <h1>CategorySalarie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ user.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('auth_user_index') }}">back to list</a>

    <a href="{{ path('auth_user_edit', {'id': category_salarie.id}) }}">edit</a>

    {{ include('settings/category_salarie/_delete_form.html.twig') }}
{% endblock %}
