{% extends 'base.html.twig' %}

{% block title %}Modifier avantage en nature{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            {{ form_start(form) }}
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title-custom" style="color: white">
                        <i data-feather="list"></i>
                        Modifier avantage en nature
                    </h5>
                </div>
                <div class="card-body">
                    {{ include('settings/aventage_nature/_form.html.twig') }}
                </div>
                <div class="card-footer">
                    <button type="submit"
                            class="btn btn-primary float-end">{{ button_label|default('Enregistrer') }}</button>
                    <a href="{{ path('setting_aventage_nature_index') }}"
                       class="btn btn-info float-end me-2">Retour</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}