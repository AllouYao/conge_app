<form method="post" action="{{ path('app_settings_aventage_nature_delete', {'id': aventage_nature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aventage_nature.id) }}">
    <button class="btn">Delete</button>
</form>
