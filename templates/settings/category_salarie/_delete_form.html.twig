<form method="post" action="{{ path('app_settings_category_salarie_delete', {'id': category_salarie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_salarie.id) }}">
    <button class="btn">Delete</button>
</form>
