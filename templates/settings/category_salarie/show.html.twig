{% extends 'base.html.twig' %}

{% block title %}CategorySalarie{% endblock %}

{% block body %}
    <h1>CategorySalarie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_salarie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_salarie.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ category_salarie.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_salarie.description }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ category_salarie.uuid }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ category_salarie.createdAt ? category_salarie.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ category_salarie.updatedAt ? category_salarie.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_settings_category_salarie_index') }}">back to list</a>

    <a href="{{ path('app_settings_category_salarie_edit', {'id': category_salarie.id}) }}">edit</a>

    {{ include('settings/category_salarie/_delete_form.html.twig') }}
{% endblock %}
