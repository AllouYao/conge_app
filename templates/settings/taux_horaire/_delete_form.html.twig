<form method="post" action="{{ path('app_settings_taux_horaire_delete', {'id': taux_horaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taux_horaire.id) }}">
    <button class="btn">Delete</button>
</form>
